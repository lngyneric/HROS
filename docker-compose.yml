services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: hros
      POSTGRES_USER: hros
      POSTGRES_PASSWORD: hros_password
    ports:
      - "5432:5432"
    volumes:
      - hros_pg:/var/lib/postgresql/data
volumes:
  hros_pg:

